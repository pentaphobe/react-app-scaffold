import Default from './default';
import Alternative from './alternative';
import utils from './utils';

const themeDictionary = {
  default: Default, 
  alternative: Alternative
};

export {
  utils
};

export default themeDictionary;